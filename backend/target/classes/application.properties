# Server port
server.port=8080

# PostgreSQL Database Settings (Supabase)
spring.datasource.url=jdbc:postgresql://aws-1-ap-northeast-2.pooler.supabase.com:5432/postgres?stringtype=unspecified
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.username=postgres.olwrtvanxnzkfykypkaq
spring.datasource.password=${DB_PASSWORD}
spring.datasource.sql-script-encoding=UTF-8

# Initialize Database from schema.sql
spring.sql.init.mode=always
spring.sql.init.schema-locations=classpath:schema.sql

# MyBatis Settings
mybatis.configuration.map-underscore-to-camel-case=true

# Logging SQL for debugging
logging.level.web=INFO
logging.level.com.example.drum.mapper=DEBUG
