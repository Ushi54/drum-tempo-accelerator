# Server port
server.port=8080

# PostgreSQL Database Settings (Supabase)
spring.datasource.url=jdbc:postgresql://db.olwrtvanxnzkfykypkaq.supabase.co:5432/postgres?stringtype=unspecified
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.username=postgres
spring.datasource.password=quvso3-zuzjeT-zazgut
spring.datasource.sql-script-encoding=UTF-8

# Initialize Database from schema.sql
spring.sql.init.mode=always
spring.sql.init.schema-locations=classpath:schema.sql

# MyBatis Settings
mybatis.configuration.map-underscore-to-camel-case=true

# Logging SQL for debugging
logging.level.web=INFO
logging.level.com.example.drum.mapper=DEBUG
